# Test Fixtures

Source-specific fixtures live in one folder per source:

```text
tests/fixtures/<source>/
```

Keep fixtures small and focused. Default tests must stay offline; capture live
source samples manually, trim them deliberately, and document lasting source
assumptions in `docs/sources.md`.

See `docs/source-fixtures.md` for the full strategy.
